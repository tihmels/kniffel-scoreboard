# End-to-end tests

Reserved for browser-level end-to-end tests (e.g. Playwright), to be added in a
later milestone once there is interactive game flow to exercise.

Unit and component tests live next to the code they cover under `src/` and run
with Vitest (`npm test`).
